print("forBreakAndContinue.txt");
Int i;
Int numberCount;

// create sorted array of sample numbers for basic break statement
Int testArray[20] = 10,20,30,40,50,60,70,80,100,200,300;
numberCount = 0;

print("Printing testArray");
for i=0 to ELEM(testArray):
    print("testArray[", i, "]=", testArray[i]);
endfor;

// Test 1: Simple break with for and if
// find the number of elements below 100 in a sorted array using a break statement
print("TEST 1: Simple break with for and if- find the number of elements below 100 in a sorted array using a break statement");

for i=0 to ELEM(testArray):
    print("The array element is: ", testArray[i]);
        if testArray[i] >= 100:
            break;
        endif;
    numberCount = numberCount + 1;
endfor;

print("Total numbers below 100: ", numberCount);

// Test 2: Another break
// Subtract 5 cents from a dollar until we get to zero using continue, we should see 20 subtractions
print("TEST 2: Another break in for and if- Subtract 5 cents from a dollar until we get to zero using continue, we should see 20 subtractions");

Int dollarCents = 100;
Int totalSubs = 0;

for i=0 to 100:
    print("Current dollar value in cents: ", dollarCents);
    if dollarCents <= 0:
        break;
    endif;
    totalSubs = totalSubs + 1;
    dollarCents = dollarCents - 5;
endfor;
print("We have reached 0. totalSubs=", totalSubs);
print("dollarCents=", dollarCents);

// Test 3: continue in for
// Print all values of array unless it is above 100, we should only see 10-80 here from testArray
print("TEST 3: Continue in for- Print all values of array unless it is above 100, we should only see 10-80 here from testArray");

for i=0 to ELEM(testArray):
    if testArray[i] >= 100:
        continue;
    endif;
    print("The array element is: ", testArray[i]);
endfor;

// Test 4: While statement with break
// search array until number is found
print("TEST 4: While statement with break- search array until number is found");

Bool bFlag = T;
i = 0;

while bFlag:
    if testArray[i] == 100:
        break;
    endif;
    i = i + 1;
endwhile;
print("Number found at testArray at index: ", i);

// Test 4: While statement with continue
// search array until number is found
print("TEST 5: While statement with continue- same as test 4 but using continue");
while bFlag:
    if testArray[i] == 100:
        bFlag = F;
        continue;
    endif;
    i = i + 1;
endwhile;
print("Number found at testArray at index: ", i);

String word = "word";
for char in word:
    print(char);
endfor;
String words[4] = "this", "has", "several", "words";
for entry in words:
    print(entry);
endfor;
