print("arrayTest.txt");

// Test 1: simple array creation
// create array and print all elements in it
print("Test 1: simple array creation- create array and print all elements in it");
Int testArray[20] = 10,20,30,40,50,60,70,80,100,200,300;
print("Printing testArray");
for i=0 to ELEM(testArray):
    print("testArray[", i, "]=", testArray[i]);
endfor;

// Test 2: change elements in array
print("Test 2: change elements in array");

testArray[5] = 1000;
testArray[7] = 3000;
for i=0 to ELEM(testArray):
    print("testArray[", i, "]=", testArray[i]);
endfor;

// Test 3: printing using subscripts
print("Test 3: printing using subscripts");
Int expr = 0;
print("Elements are:", testArray[expr], testArray[expr+2], testArray[expr+5], testArray[expr+5-2]);

// Test 4: initializing array with no values
print("Test 4: initializing array with no values");

print("This array is empty right now");
Int emptyArray[10];
print("Number of elements in empty array", ELEM(emptyArray));

emptyArray[expr+1] = 20000; // assign to an uninitialized array
print("After assigning to empty array:", emptyArray[expr+1]);

// Test 5: Make a copy of an existing array
print("Test 5: Make a copy of an existing array");

Int copyArray[8];
copyArray = testArray;
print("Printing copyArray which should only show 8 elements total");
for i=0 to ELEM(copyArray):
    print("copyArray[", i, "]=", copyArray[i]);
endfor;